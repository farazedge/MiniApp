<view class="container log-list">
  <block wx:for="{{userData.userData}}" wx:for-item="log" wx:key="*this">
    <text class="log-item">{{index + 1}}. {{log.name}}</text>
    <text class="log-item">{{index + 1}}. {{log.contact}}</text>
  </block>
</view>